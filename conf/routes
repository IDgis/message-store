# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET		/messages/:messageKey		controllers.MessageStore.get(messageKey)
POST	/messages					controllers.MessageStore.put()
PUT		/messages					controllers.MessageStore.put()